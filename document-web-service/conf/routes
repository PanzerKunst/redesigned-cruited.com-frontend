# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /api/docs/:orderId/cv                           controllers.Application.getCvOfOrder(orderId: Long)
GET     /api/docs/:orderId/cover-letter                 controllers.Application.getCoverLetterOfOrder(orderId: Long)
GET     /api/docs/:orderId/linkedin-profile             controllers.Application.getLinkedinProfileOfOrder(orderId: Long)
